# Pinned versions for reproducing the published numbers.
#
# The CPU pins below are the environment the paper-number gate
# (`make test`) passes under: they are sufficient to rebuild every figure
# and recompute the paper claims covered by the release gate from
# `data/aggregated/`.
#
#     pip install -r requirements.txt && make test
#
# GPU scoring is a separate stack, listed at the bottom. It is not needed to
# reproduce the paper from the released aggregates, only to re-run the sweep.

# analysis, figures, tests (CPU)
numpy==1.26.0
pandas==2.2.3
scipy==1.11.3
scikit-learn==1.3.2
statsmodels==0.14.4
pyarrow==19.0.1
pyyaml==6.0.3
matplotlib==3.8.2
tqdm==4.67.3
datasets==3.0.0
huggingface-hub==0.25.1
pytest==9.0.3
ruff==0.16.5

# Recorded GPU environments (`pip install -e .[gpu]` provides a compatible stack):
# - probing and intervention: Python 3.12.13, torch==2.10.0+cu128,
#   transformers==5.0.0
# - benchmark scoring: Python 3.12.13, torch==2.11.0+cu128,
#   transformers==5.12.1
# - selected framing conditions: Python 3.13.15,
#   transformers==5.15.1 or 5.16.1
# Scoring is a deterministic likelihood comparison; the scoring environments
# agree to bfloat16 precision.
